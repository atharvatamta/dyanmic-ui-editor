import { Box } from "lucide-react";

export default function ViewInRoom() {
  return (
    <button className="view-in-room">
      <Box size={18} />
      <span>View in your room</span>
    </button>
  );
}
